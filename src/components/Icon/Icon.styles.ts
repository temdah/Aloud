import { StyleSheet } from 'react-native';
import type { Palette } from '../../theme';

// Raw SVG primitives; no static StyleSheet styles.
export const makeStyles = (_p: Palette) => StyleSheet.create({});
