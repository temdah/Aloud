import { StyleSheet } from 'react-native';
import type { Palette } from '../../theme';

// Icon renders raw SVG primitives; it carries no static StyleSheet styles.
export const makeStyles = (_p: Palette) => StyleSheet.create({});
